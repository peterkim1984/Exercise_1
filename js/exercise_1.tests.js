describe('A1 Array Length', function () {
    it('checks if the A1 array is the correct length', function () {
        expect(a1.length).toEqual(1000);
    });
});

describe('A2 Array Length', function () {
    it('checks if the A2 array is the correct length', function () {
        expect(a2.length).toEqual(1000);
    });
});

describe('Index 0 compare', function () {
    it('checks that a1 and a2 are different at index 0', function () {
        expect(a1[0]).not.toEqual(a2[0]);
    });
});

describe('Index 1 compare', function () {
    it('checks that a1 and a2 are different at index 1', function () {
        expect(a1[1]).not.toEqual(a2[1]);
    });
});

describe('Index 2 compare', function () {
    it('checks that a1 and a2 are different at index 2', function () {
        expect(a1[2]).not.toEqual(a2[2]);
    });
});


describe('Index 3 compare', function () {
    it('checks that a1 and a2 are different at index 3', function () {
        expect(a1[3]).not.toEqual(a2[3]);
    });
});


describe('Index 4 compare', function () {
    it('checks that a1 and a2 are different at index 4', function () {
        expect(a1[4]).not.toEqual(a2[4]);
    });
});


describe('Index 5 compare', function () {
    it('checks that a1 and a2 are different at index 5', function () {
        expect(a1[5]).not.toEqual(a2[5]);
    });
});



